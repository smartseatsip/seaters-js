import * as _app from '../../seaters-api/app/app-types';

export namespace app {
  export interface Env extends _app.Env {
  }
  export interface BuildInfo extends _app.BuildInfo {
  }
  export interface AlgoliaConfiguration extends _app.AlgoliaConfiguration {
  }
  export interface Country extends _app.Country {
  }
  export interface Language extends _app.Language {
  }
  export interface Translation extends _app.Translation {
  }
  export interface TimeZone extends _app.TimeZone {
  }
  export interface Currency extends _app.Currency {
  }
  export type TRANSLATION_TARGET = _app.TRANSLATION_TARGET;
}
