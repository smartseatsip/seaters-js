require('../../helpers');

describe('PUT /api/fan/waiting-lists', function () {

  /**
   * TODO: api-context should handle endpoint params first
   * TODO: login as fan related to this WL
   */

  /*
   it('should return 200-OK on valid waitinglist id', function(done) {
   var listId = '3a910508-4962-4e38-b468-9d5b74a73ac5';
   client.api.fan.waitingList(listId)
   .then((wl) => {
   expect(wl['waitingListId']).toMatch(listId);
   }, apiFail() )
   .then(done, shouldNotCatch(done));
   });
   */
});
